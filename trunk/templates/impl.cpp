/*!
    \file src/cpaf/$(PORT_LOWER)/$(NAME_LOWER).$(IMPL_SRC_EXT)
    \brief Implementation of cpaf::$(PORT_LOWER)::gui::$(NAME)
    \date Created: $(DATE)
*/

#include <cpaf/$(PORT_LOWER)/gui/$(NAME_LOWER).h>

$(IF CONSTRUCTABLE)
void cpaf::$(PORT_LOWER)::gui::$(NAME)::create(const cpaf::gui::initializer::$(NAME)Data &params)
{

}
$(END)
